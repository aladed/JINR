# RAG / LLM remediation module dependencies
pydantic>=2.0,<3
numpy>=1.24,<3

# Optional: local embeddings (falls back to bag-of-words if missing)
sentence-transformers>=2.2,<4

# Optional: local LLM via Ollama
ollama>=0.3,<1

# Optional: CPU-friendly transformers pipeline
transformers>=4.36,<5
torch>=2.0,<3

# Testing
pytest>=7.4,<9
